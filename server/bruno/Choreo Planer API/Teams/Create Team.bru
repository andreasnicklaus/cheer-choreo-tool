meta {
  name: Create Team
  method: POST
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/team/
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Glamorous Blush",
    "clubId": "{{id}}",
    "seasonId": "{{id}}"
  }
}