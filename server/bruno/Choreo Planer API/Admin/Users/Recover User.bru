meta {
  name: Recover User
  method: POST
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/admin/users/recover
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "id": "{{id}}"
  }
}
