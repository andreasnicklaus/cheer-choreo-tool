<!DOCTYPE html>
<html lang="<%- locals.locale? locale : 'en' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= t('errors.unknown-error') %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <style>
      body {
        text-align: center;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
        display: flex;
        flex-direction: column;
        justify-content: center;
        height: 100vh;
        padding: 0px 50px;
      }
      a[type="button"] {
        background-color: #0069d9;
        color: white;
        padding: 16px 24px;
        border-radius: 4px;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <h1>😢 <%= t('error.oh-no') %></h1>
    <p style="margin-bottom: 0"><%= t('error.info-text-1') %></p>
    <p style="margin-top: 0"><%= t('error.info-text-2') %></p>

    <div
      style="
        margin: 32px 0px;
        display: flex;
        column-gap: 20px;
        justify-content: center;
      "
    >
      <a type="button" href="#" onclick="window.location.reload()">
        <i class="bi bi-arrow-counterclockwise"></i>
        <%= t('error.reload') %>
      </a>
      <a
        type="button"
        href="mailto:info@choreo-planer.de?cc=admin@choreo-planer.de&subject=Ich möchte einen Fehler melden&body=<%= emailBody %>"
      >
        <i style="margin-right: 8px" class="bi bi-send"></i>
        <%= t('status.kontakt-aufnehmen') %>
      </a>
    </div>

    <details style="text-align: start">
      <summary style="text-align: center"><%= t('error.error-info') %></summary>
      <p><%= t('error.action') %> <%= action %></p>
      <p><%= t('error.data') %> <%= data %></p>
      <p><%= t('error.message') %> <%= error %></p>
      <p><%= t('error.timestamp') %> <%= timestamp %></p>
    </details>
  </body>
</html>
