<!DOCTYPE html>
<html lang="en">
  <head>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.3/dist/confetti.browser.min.js"></script>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= t('status.server-status') %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <style>
      body {
        text-align: center;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
        display: flex;
        flex-direction: column;
        justify-content: center;
        height: 100vh;
        padding: 0px 50px;
        margin: 0;
      }
      a[type="button"] {
        background-color: #0069d9;
        color: white;
        padding: 16px 24px;
        border-radius: 4px;
        text-decoration: none;
      }
      #adminLink {
        position: fixed;
        bottom: 10px;
        right: 10px;
        padding: 20px;
        box-shadow: #00000073 0 0 70px;
        border-radius: 50%;
        aspect-ratio: 1 / 1;
        width: 20px;
        height: 20px;
        color: #00000073;
      }
    </style>
  </head>
  <body>
    <h1>🎉 <%= t('status.server-sind-online') %></h1>
    <p><%= t('status.aktuelle-version')%> <%= version %></p>

    <div
      style="
        margin: 12px 0px;
        display: flex;
        column-gap: 20px;
        justify-content: center;
      "
    >
      <a type="button" href="<%= frontendDomain %>" target="_blank">
        <%= t('status.zum-choreo-planer') %>
      </a>
    </div>
    <a href="mailto:info@choreo-planer.de"
      ><%= t('status.kontakt-aufnehmen') %></a
    >
    <a href="/admin" id="adminLink"> <i class="bi bi-person-fill-gear"></i></a>

    <script defer nonce="<%= cspNonce %>">
      confetti({
        particleCount: 200,
        spread: 100,
        origin: { y: 0.8 },
      });
    </script>
  </body>
</html>
